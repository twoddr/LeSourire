# Maven
target/
.flattened-pom.xml

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# Système
*.log
.DS_Store

# Fichiers locaux d'exécution
# La clé de chiffrement, elle, est VERSIONNÉE : elle doit être livrée avec le
# code, sinon un serveur packagé en génère une autre et rend illisibles les
# données déjà chiffrées (cf. README, « Chiffrement des données sensibles »).
fichiers/*
!fichiers/cle-chiffrement.key
sauvegardes/

# Packs de déploiement générés
dist/
out/

# Archives de livraison déposées à la racine (les paquets vivent dans out/)
/*.tar.gz
/*.zip
