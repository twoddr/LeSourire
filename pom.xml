<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <groupId>com.lesourire</groupId>
  <artifactId>lesourire</artifactId>
  <!-- Une seule source de vérité : propriété <revision> ci-dessous.
         Les modules enfants référencent ${revision} — ne changez que cette valeur. -->
  <version>${revision}</version>
  <packaging>pom</packaging>
  <name>Le Sourire</name>
  <description>Logiciel de gestion du cabinet dentaire Le Sourire (Douala)</description>
  <modules>
    <module>commun</module>
    <module>serveur</module>
    <module>client</module>
  </modules>

  <properties>
    <!-- === Version produit (seul endroit à modifier pour une release) === -->
    <revision>0.1.3</revision>
    <maven.compiler.release>21</maven.compiler.release>
    <!-- Conserve les noms de paramètres à la compilation (requis par Spring MVC) -->
    <maven.compiler.parameters>true</maven.compiler.parameters>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
    <spring-boot.version>3.5.3</spring-boot.version>
    <javafx.version>21.0.11</javafx.version>
    <atlantafx.version>2.0.1</atlantafx.version>
    <ikonli.version>12.4.0</ikonli.version>
    <!-- Aligné sur jackson-bom de Spring Boot 3.5.3 (évite databind 2.22 + annotations 2.19) -->
    <jackson.version>2.19.1</jackson.version>
    <!-- Factures PDF : HTML/CSS (Thymeleaf) → OpenHTMLtoPDF -->
    <openhtmltopdf.version>1.0.10</openhtmltopdf.version>
    <thymeleaf.version>3.1.3.RELEASE</thymeleaf.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-dependencies</artifactId>
        <version>${spring-boot.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>
      <dependency>
        <groupId>com.lesourire</groupId>
        <artifactId>lesourire-commun</artifactId>
        <version>${project.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <plugins>
      <!-- Résout ${revision} dans les POM installés (Maven CI Friendly Versions) -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>flatten-maven-plugin</artifactId>
        <version>1.6.0</version>
        <configuration>
          <updatePomFile>true</updatePomFile>
          <flattenMode>resolveCiFriendliesOnly</flattenMode>
        </configuration>
        <executions>
          <execution>
            <id>flatten</id>
            <phase>process-resources</phase>
            <goals>
              <goal>flatten</goal>
            </goals>
          </execution>
          <execution>
            <id>flatten.clean</id>
            <phase>clean</phase>
            <goals>
              <goal>clean</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
